 <table class="table-hover">
                <thead>
                    <tr>
                        <th>Link</th>
                        <th>Player</th>
                        <th>Player</th>
                        <th>Deck</th>
                        <th>Deck</th>
                    </tr>
                </thead>
                <tbody>
                {{#each this.Rounds}} 
                    <tr>
                        <td><a href="{{ this.coverage_link }}"> <img src"{{ providerImg }}"> {{ this.round_number }} </a></td>
                        <td><a href="{{ player1url }}"> {{ this.player1_name }} </a></td>
                        <td><a href="{{ player2url }}"> {{ this.player2_name }} </a></td>
                        <td>{{ player1_deck }}</td>
                        <td>{{ player2_deck }}</td>
                    </tr>
                {{/each}}
                </tbody>

            </table>  <!--end table-hover -->
