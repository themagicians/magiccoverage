<div class="container" id="responseDiv">
    {{#each events}}
        <div class="jumbotron" id="{{ ID }}">
            <h2> {{events.event_name}} </h2> <br>
            <h3> {{events.event_date}} </h3> <br>
            <h3> {{events.format}} </h3> <br>

            <div class="table-responsive">
                {{> table-partial}}
            </div>

        </div> {{!-- end jumbotron --}}
    {{/each}} {{!-- end #each events --}}
</div>